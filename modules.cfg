[activate]
# Modules to install/upgrade on deployment.
# Platform modules (llm suite, whatsapp, billing, OCA, etc.) are pre-installed
# in the base image — list only modules your custom addons introduce.

base
web
contacts
product
account
mail
