[activate]
# All platform dependencies (core, OCA, LLM suite, storage, WhatsApp, billing)
# are declared in the 'ordomatics' module and installed transitively.
# Client-specific providers (llm_fal_ai, llm_replicate) and web_enterprise
# are declared as dependencies of the 'smartacus' module.
smartacus
